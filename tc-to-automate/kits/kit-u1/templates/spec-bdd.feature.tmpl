# Generated by automation-kit | Kit: {{kitId}} | {{generatedDate}}
# Source test cases: {{testCaseIds}}
# Regenerate with: /generate-tests --bdd or /add-test --bdd

Feature: {{featureName}}

  {{#each scenarios}}
  {{#each this.tags}}@{{this}} {{/each}}
  Scenario: {{this.id}} - {{this.title}}
    {{#each this.given}}
    Given {{this}}
    {{/each}}
    {{#each this.when}}
    {{#if @first}}When {{else}}And  {{/if}}{{this}}
    {{/each}}
    {{#each this.then}}
    {{#if @first}}Then {{else}}And  {{/if}}{{this}}
    {{/each}}

  {{/each}}
